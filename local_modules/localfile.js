// const sayHello = require('./localmodule'); // call the other filename as the params in this function
// sayHello(); 


const math = require('./math');
console.log(math.add(10, 20)); // math is a variable name not js math.
console.log(math.sub(10, 20));

